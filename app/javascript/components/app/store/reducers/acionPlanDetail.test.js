//import {cloneableGenerator} from '@redux-saga/testing-utils'
// import {actionPlanDetailSaga, loadActionPlanData} from "./actionPlanDetail";

describe('with redux-saga native testing', () => {
    // const generator = cloneableGenerator(actionPlanDetailSaga())(loadActionPlanData(1))

    test('gets the execution context', () => {
       // const result = generator.next().value
       // console.log(result)
    })

})